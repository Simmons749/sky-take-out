sky:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: localhost
    port: 3306
    database: sky_take_out
    username: root
    password: root
  alioss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    access-key-id: ${ACCESS_KEY_ID}
    access-key-secret: ${ACCESS_KEY_SECRET}
    bucket-name: shx-sky-itcast
  redis:
    host: localhost
    port: 6379
